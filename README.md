# bookstore-microservice-demo
基于Spring Cloud Alibaba的微服务图书商城，包含单体对照组及前端

#前端项目（bookstore-frontend）
1. 进入前端目录：`cd bookstore-frontend`
2. 安装依赖：`npm install`（这会自动生成 `node_modules` 文件夹）
3. 启动开发服务器：`npm run dev`
4. 访问 `http://localhost:5173`
> 注：`node_modules` 文件夹未提交至仓库，请执行 `npm install` 自行安装。
